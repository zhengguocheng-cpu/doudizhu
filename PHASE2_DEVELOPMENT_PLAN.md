# 阶段二开发计划 - 游戏核心玩法

**开始时间**: 2025-10-27 22:08  
**预计完成**: TBD  
**状态**: 🚧 进行中

---

## 📋 总体目标

实现斗地主游戏的核心玩法，包括地主牌显示、出牌功能、游戏进行UI和结算功能。

---

## 🎯 开发优先级

### 第一优先：地主牌显示 + 出牌功能 ⭐⭐⭐
**重要性**: 游戏核心玩法，用户最关心的功能

### 第二优先：游戏进行中的UI优化 ⭐⭐
**重要性**: 提升用户体验，让游戏更直观

### 第三优先：游戏结算 ⭐
**重要性**: 完整的游戏闭环，可以开始真正玩游戏

---

## 📅 详细开发计划

## 第一阶段：地主牌显示 + 出牌功能

### 1.1 地主牌显示 (2-3小时)

#### 功能需求
- [ ] 地主确定后，在桌面中央显示3张底牌
- [ ] 底牌飞向地主玩家的动画效果
- [ ] 地主玩家手牌增加3张底牌
- [ ] 地主标识更明显（图标、边框、特效）

#### 技术实现
```javascript
// 前端监听地主确定事件
socket.on('landlord_determined', (data) => {
    // 1. 显示3张底牌
    showLandlordCards(data.landlordCards);
    
    // 2. 播放底牌飞向地主的动画
    animateCardsToLandlord(data.landlordCards, data.landlordId);
    
    // 3. 更新地主手牌
    if (data.landlordId === currentPlayerId) {
        addCardsToHand(data.landlordCards);
    }
    
    // 4. 显示地主标识
    showLandlordBadge(data.landlordId);
});
```

#### UI设计
```
桌面中央区域：
┌─────────────────────┐
│   [底牌1][底牌2][底牌3]   │ ← 3张底牌展示3秒
│                     │
│   👑 XXX 成为地主！   │ ← 提示信息
└─────────────────────┘

地主标识：
┌────────┐
│ 👑     │ ← 地主图标
│ 玩家名  │
│ 已准备  │
└────────┘
```

#### 测试用例
- [ ] 测试1：地主确定后，3张底牌正确显示
- [ ] 测试2：底牌动画流畅，飞向正确的玩家
- [ ] 测试3：地主手牌正确增加3张
- [ ] 测试4：地主标识清晰可见
- [ ] 测试5：非地主玩家看到相同效果

---

### 1.2 出牌类型识别 (3-4小时)

#### 功能需求
- [ ] 识别单牌
- [ ] 识别对子
- [ ] 识别三张
- [ ] 识别三带一
- [ ] 识别三带二
- [ ] 识别顺子（5张及以上）
- [ ] 识别连对（3对及以上）
- [ ] 识别飞机（2个三张及以上）
- [ ] 识别炸弹（4张相同）
- [ ] 识别王炸（大王+小王）

#### 技术实现
```javascript
class CardTypeDetector {
    /**
     * 检测牌型
     */
    static detect(cards) {
        if (cards.length === 0) return null;
        
        // 按优先级检测
        if (this.isRocket(cards)) return { type: 'ROCKET', ... };
        if (this.isBomb(cards)) return { type: 'BOMB', ... };
        if (this.isSingle(cards)) return { type: 'SINGLE', ... };
        if (this.isPair(cards)) return { type: 'PAIR', ... };
        if (this.isTriple(cards)) return { type: 'TRIPLE', ... };
        if (this.isTriplePlusOne(cards)) return { type: 'TRIPLE_PLUS_ONE', ... };
        if (this.isTriplePlusTwo(cards)) return { type: 'TRIPLE_PLUS_TWO', ... };
        if (this.isStraight(cards)) return { type: 'STRAIGHT', ... };
        if (this.isDoubleStraight(cards)) return { type: 'DOUBLE_STRAIGHT', ... };
        if (this.isPlane(cards)) return { type: 'PLANE', ... };
        
        return null; // 无效牌型
    }
}
```

#### 测试用例
- [ ] 测试单牌：♠3
- [ ] 测试对子：♠3, ♥3
- [ ] 测试三张：♠3, ♥3, ♦3
- [ ] 测试三带一：♠3, ♥3, ♦3, ♣4
- [ ] 测试三带二：♠3, ♥3, ♦3, ♣4, ♠4
- [ ] 测试顺子：♠3, ♥4, ♦5, ♣6, ♠7
- [ ] 测试连对：♠3, ♥3, ♦4, ♣4, ♠5, ♥5
- [ ] 测试飞机：♠3, ♥3, ♦3, ♣4, ♠4, ♥4
- [ ] 测试炸弹：♠3, ♥3, ♦3, ♣3
- [ ] 测试王炸：大王, 小王

---

### 1.3 出牌验证 (2-3小时)

#### 功能需求
- [ ] 验证是否轮到当前玩家
- [ ] 验证牌型是否有效
- [ ] 验证是否能压过上家的牌
- [ ] 首次出牌（地主先出）
- [ ] 跟牌规则验证

#### 技术实现
```javascript
class CardValidator {
    /**
     * 验证出牌是否合法
     */
    static validate(cards, lastPlay, isFirstPlay) {
        // 1. 检测牌型
        const cardType = CardTypeDetector.detect(cards);
        if (!cardType) {
            return { valid: false, reason: '无效的牌型' };
        }
        
        // 2. 首次出牌（任意牌型）
        if (isFirstPlay) {
            return { valid: true, cardType };
        }
        
        // 3. 炸弹和王炸可以压任何牌
        if (cardType.type === 'BOMB' || cardType.type === 'ROCKET') {
            return { valid: true, cardType };
        }
        
        // 4. 必须是相同牌型
        if (cardType.type !== lastPlay.type) {
            return { valid: false, reason: '牌型不匹配' };
        }
        
        // 5. 必须比上家大
        if (cardType.value <= lastPlay.value) {
            return { valid: false, reason: '牌太小，压不过' };
        }
        
        return { valid: true, cardType };
    }
}
```

#### 测试用例
- [ ] 测试首次出牌（任意牌型）
- [ ] 测试跟牌（相同牌型，更大）
- [ ] 测试跟牌失败（牌型不同）
- [ ] 测试跟牌失败（牌太小）
- [ ] 测试炸弹压牌
- [ ] 测试王炸压牌

---

### 1.4 出牌UI和交互 (3-4小时)

#### 功能需求
- [ ] 选择要出的牌（点击选中/取消）
- [ ] 出牌按钮（验证后发送）
- [ ] 不出按钮（跳过本轮）
- [ ] 提示按钮（智能提示可出的牌）
- [ ] 出牌动画（牌飞到桌面中央）
- [ ] 显示上家出的牌

#### UI设计
```
游戏控制区：
┌─────────────────────────────┐
│  [提示]  [出牌]  [不出]       │
│                             │
│  已选择: 3张牌               │
└─────────────────────────────┘

桌面出牌区：
┌─────────────────────────────┐
│  上家出牌: [♠3][♥3][♦3]      │
│  牌型: 三张                  │
└─────────────────────────────┘
```

#### 技术实现
```javascript
// 出牌按钮点击
playCardsBtn.addEventListener('click', () => {
    const selectedCards = getSelectedCards();
    
    // 验证牌型
    const validation = CardValidator.validate(
        selectedCards, 
        lastPlay, 
        isFirstPlay
    );
    
    if (!validation.valid) {
        showMessage(validation.reason);
        return;
    }
    
    // 发送出牌事件
    socket.emit('play_cards', {
        cards: selectedCards,
        cardType: validation.cardType
    });
    
    // 播放出牌动画
    animateCardsToTable(selectedCards);
});

// 提示按钮点击
hintBtn.addEventListener('click', () => {
    const hints = CardHintGenerator.generate(
        playerHand, 
        lastPlay
    );
    
    if (hints.length === 0) {
        showMessage('没有可出的牌');
    } else {
        highlightCards(hints[0]); // 高亮第一个提示
    }
});
```

#### 测试用例
- [ ] 测试选择卡牌（单选、多选）
- [ ] 测试出牌成功
- [ ] 测试出牌失败（提示错误信息）
- [ ] 测试不出按钮
- [ ] 测试提示功能
- [ ] 测试出牌动画

---

### 1.5 出牌提示功能 (2-3小时)

#### 功能需求
- [ ] 分析当前手牌
- [ ] 根据上家出牌生成提示
- [ ] 智能排序（优先出小牌）
- [ ] 支持多种牌型提示

#### 技术实现
```javascript
class CardHintGenerator {
    /**
     * 生成出牌提示
     */
    static generate(hand, lastPlay) {
        const hints = [];
        
        if (!lastPlay) {
            // 首次出牌，推荐出最小的单牌
            hints.push(this.getSmallestSingle(hand));
        } else {
            // 根据上家牌型生成提示
            switch (lastPlay.type) {
                case 'SINGLE':
                    hints.push(...this.findBiggerSingles(hand, lastPlay));
                    break;
                case 'PAIR':
                    hints.push(...this.findBiggerPairs(hand, lastPlay));
                    break;
                case 'TRIPLE':
                    hints.push(...this.findBiggerTriples(hand, lastPlay));
                    break;
                // ... 其他牌型
            }
            
            // 添加炸弹提示
            hints.push(...this.findBombs(hand));
        }
        
        return hints;
    }
}
```

#### 测试用例
- [ ] 测试首次出牌提示
- [ ] 测试跟单牌提示
- [ ] 测试跟对子提示
- [ ] 测试跟三张提示
- [ ] 测试炸弹提示
- [ ] 测试无牌可出

---

## 第二阶段：游戏进行中的UI优化

### 2.1 当前玩家指示 (1-2小时)

#### 功能需求
- [ ] 高亮当前出牌玩家
- [ ] 显示"轮到你了"提示
- [ ] 倒计时显示（30秒）
- [ ] 超时自动跳过

#### UI设计
```
当前玩家高亮：
┌────────────┐
│ ⏰ 25秒    │ ← 倒计时
│ 👤 玩家名   │
│ 🎯 出牌中   │ ← 状态指示
└────────────┘
```

---

### 2.2 出牌历史显示 (1-2小时)

#### 功能需求
- [ ] 显示最近3轮出牌
- [ ] 显示出牌玩家名字
- [ ] 显示牌型名称
- [ ] 自动滚动

#### UI设计
```
出牌历史：
┌─────────────────────────┐
│ 玩家A: [♠3][♥3] (对子)   │
│ 玩家B: 不出              │
│ 玩家C: [♠4][♥4] (对子)   │
└─────────────────────────┘
```

---

### 2.3 剩余牌数显示 (1小时)

#### 功能需求
- [ ] 显示每个玩家剩余牌数
- [ ] 剩余牌数少于5张时高亮提示
- [ ] 报警动画（剩余1-2张）

---

### 2.4 游戏音效 (2-3小时)

#### 功能需求
- [ ] 出牌音效
- [ ] 炸弹音效
- [ ] 王炸音效
- [ ] 胜利音效
- [ ] 失败音效
- [ ] 背景音乐

---

## 第三阶段：游戏结算

### 3.1 游戏结束判断 (1-2小时)

#### 功能需求
- [ ] 检测玩家手牌为空
- [ ] 判断地主胜利/农民胜利
- [ ] 计算游戏时长
- [ ] 记录游戏数据

---

### 3.2 结算界面 (2-3小时)

#### 功能需求
- [ ] 显示胜负结果
- [ ] 显示积分变化
- [ ] 显示游戏统计
- [ ] 再来一局按钮
- [ ] 返回大厅按钮

#### UI设计
```
结算界面：
┌─────────────────────────┐
│     🎉 游戏结束！        │
│                         │
│   地主胜利 / 农民胜利    │
│                         │
│   玩家A: +10分 👑        │
│   玩家B: -5分           │
│   玩家C: -5分           │
│                         │
│   游戏时长: 5分30秒      │
│                         │
│  [再来一局]  [返回大厅]  │
└─────────────────────────┘
```

---

### 3.3 积分系统 (2-3小时)

#### 功能需求
- [ ] 基础分计算（底分×倍数）
- [ ] 炸弹加倍
- [ ] 春天加倍
- [ ] 反春加倍
- [ ] 积分持久化

#### 计算规则
```
基础分 = 底分(1) × 地主倍数(1-3)
炸弹倍数 = 2^炸弹数量
春天倍数 = 2（农民一张未出）
反春倍数 = 2（地主一张未出）

最终积分 = 基础分 × 炸弹倍数 × 春天倍数
```

---

## 🧪 测试策略

### 单元测试
```javascript
// 牌型检测测试
describe('CardTypeDetector', () => {
    test('应该识别单牌', () => {
        const cards = ['♠3'];
        const result = CardTypeDetector.detect(cards);
        expect(result.type).toBe('SINGLE');
    });
    
    test('应该识别对子', () => {
        const cards = ['♠3', '♥3'];
        const result = CardTypeDetector.detect(cards);
        expect(result.type).toBe('PAIR');
    });
    
    // ... 更多测试
});

// 出牌验证测试
describe('CardValidator', () => {
    test('应该允许首次出牌', () => {
        const cards = ['♠3'];
        const result = CardValidator.validate(cards, null, true);
        expect(result.valid).toBe(true);
    });
    
    test('应该拒绝无效牌型', () => {
        const cards = ['♠3', '♥4'];
        const result = CardValidator.validate(cards, null, true);
        expect(result.valid).toBe(false);
    });
    
    // ... 更多测试
});
```

### 集成测试
```javascript
// 完整游戏流程测试
describe('Game Flow', () => {
    test('完整游戏流程', async () => {
        // 1. 3个玩家加入
        await joinGame('Player1');
        await joinGame('Player2');
        await joinGame('Player3');
        
        // 2. 开始游戏
        await startGame();
        
        // 3. 抢地主
        await bidLandlord('Player1', true);
        await bidLandlord('Player2', false);
        await bidLandlord('Player3', false);
        
        // 4. 地主出牌
        await playCards('Player1', ['♠3']);
        
        // 5. 其他玩家跟牌或不出
        await pass('Player2');
        await playCards('Player3', ['♥3']);
        
        // ... 继续游戏直到结束
        
        // 6. 验证结算
        const result = await getGameResult();
        expect(result.winner).toBeDefined();
    });
});
```

### 自动化测试
```bash
# 运行所有测试
npm test

# 运行特定测试
npm test -- CardTypeDetector

# 生成覆盖率报告
npm test -- --coverage
```

---

## 📊 进度追踪

### 第一阶段：地主牌显示 + 出牌功能
- [ ] 1.1 地主牌显示 (0/5)
- [ ] 1.2 出牌类型识别 (0/10)
- [ ] 1.3 出牌验证 (0/6)
- [ ] 1.4 出牌UI和交互 (0/6)
- [ ] 1.5 出牌提示功能 (0/6)

**预计时间**: 12-17小时  
**实际时间**: TBD

### 第二阶段：游戏进行中的UI优化
- [ ] 2.1 当前玩家指示 (0/4)
- [ ] 2.2 出牌历史显示 (0/4)
- [ ] 2.3 剩余牌数显示 (0/3)
- [ ] 2.4 游戏音效 (0/6)

**预计时间**: 5-8小时  
**实际时间**: TBD

### 第三阶段：游戏结算
- [ ] 3.1 游戏结束判断 (0/4)
- [ ] 3.2 结算界面 (0/5)
- [ ] 3.3 积分系统 (0/5)

**预计时间**: 5-8小时  
**实际时间**: TBD

---

## 🎯 里程碑

### Milestone 1: 基础出牌功能
- [ ] 完成地主牌显示
- [ ] 完成出牌类型识别
- [ ] 完成出牌验证
- [ ] 完成基础出牌UI

**目标日期**: TBD

### Milestone 2: 完整游戏体验
- [ ] 完成出牌提示
- [ ] 完成游戏进行UI
- [ ] 完成音效系统

**目标日期**: TBD

### Milestone 3: 游戏闭环
- [ ] 完成游戏结算
- [ ] 完成积分系统
- [ ] 完成再来一局功能

**目标日期**: TBD

---

## 📝 开发日志

### 2025-10-27
- ✅ 创建阶段二开发计划
- 🚧 准备开始第一阶段开发

---

## 🔗 相关文档

- [阶段一完成总结](./PHASE1_COMPLETION_SUMMARY.md)
- [程序架构文档](./PROGRAM_ARCHITECTURE.md)
- [事件流程指南](./EVENT_FLOW_GUIDE.md)
- [代码理解指南](./CODE_UNDERSTANDING_GUIDE.md)

---

**文档创建时间**: 2025-10-27 22:08  
**文档版本**: v1.0  
**状态**: 🚧 进行中
