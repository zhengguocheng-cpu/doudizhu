export declare class CardValidator {
    static validatePlayerCards(playerCards: string[], playedCards: string[]): boolean;
    static validateCardCount(cards: string[]): boolean;
    static isValidCardString(cardString: string): boolean;
    static validateRoomPlayerCount(playerCount: number): boolean;
    static validateGameStartConditions(players: any[]): boolean;
    static isPass(cards: string[]): boolean;
    static getCardValue(cardString: string): number;
    static compareCards(card1: string, card2: string): number;
}
//# sourceMappingURL=cardValidator.d.ts.map